import { PropertiesconectinglineDirective } from './propertiesconectingline.directive';

describe('PropertiesconectinglineDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertiesconectinglineDirective();
    expect(directive).toBeTruthy();
  });
});
